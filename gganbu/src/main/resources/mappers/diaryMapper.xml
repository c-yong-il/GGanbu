<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gg.mapper.DiaryMapper">

	<select id="selectDiary" parameterType="String" resultType="DiaryDTO">
		
		select diary_num, mem_id, diary_content, diary_date, diary_updatedate, diary_status 
		from diary_table where mem_id=#{mem_id} order by diary_date desc limit 1
	
	</select>
	
	<select id="now" resultType="String">
	
		select DATE_FORMAT(now(), '%Y-%m-%d')
		
	</select> 

</mapper>